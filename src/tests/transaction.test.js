const expect = require('chai').expect;
describe('TRANSACTIONS', function () {
    it('Should Pass (dummy test)', function () {
        expect(1).to.eq(1);
    })
    it('should insert transaction', function () {

    })
});